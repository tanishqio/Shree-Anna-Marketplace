/**
 * Voice components — TTS, STT, voice-enabled registration
 * import { VoiceButton, SpeakButton } from '@/components/voice'
 */
export { VoiceButton } from '@/components/VoiceButton';
export { SpeakButton } from '@/components/SpeakButton';
